name: rocket_apps_io
description: Files, directories, data streams and path manipulation

publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  shared_preferences: ^2.0.8
  path_provider: ^2.0.6
  
dev_dependencies:
  flutter_test:
    sdk: flutter